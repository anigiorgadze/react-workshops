const home ='/';
const universitiesList = '/results/:country'
const details = '/university/:name'
const notFound = '*';


export const routes = {
    home,
    universitiesList,
    details,
    notFound
}